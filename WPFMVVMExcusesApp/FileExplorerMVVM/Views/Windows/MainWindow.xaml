<Window x:Class="FileExplorerMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileExplorerMVVM"
        xmlns:cc="clr-namespace:FileExplorerMVVM.Infrastructure.CustomControls"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="782" Width="1235"
        MinHeight="760" MinWidth="864"
        WindowStartupLocation="CenterScreen"
        x:Name="MyMainWindow">
    
    <Window.Resources>
        <BitmapImage x:Key="Image_1" UriSource="Castle.jpg"/>
        <BitmapImage x:Key="Image_2" UriSource="Ice.jpg"/>
        <BitmapImage x:Key="Image_3" UriSource="Ivasik.jpg"/>
        <BitmapImage x:Key="Image_4" UriSource="Mount.jpg"/>
        <BitmapImage x:Key="Image_5" UriSource="Sun.jpg"/>
    </Window.Resources>
    
    <Window.Style>
        <Style TargetType="{x:Type Window}">
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome
                        CaptionHeight="32"
                        CornerRadius="0"
                        GlassFrameThickness="0"
                        NonClientFrameEdges="None"
                        ResizeBorderThickness="1"
                        UseAeroCaptionButtons="False"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        <Border
                            x:Name="RootBorder"
                            BorderBrush="Firebrick"
                            BorderThickness="0"
                            Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                
                                <!--#region TitleBar-->
                                <Grid x:Name="TitleBarGrid"
                                      Grid.Row="0"
                                      Grid.Column="1"
                                      Height="32">
                                    <TextBlock Name="Title"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               Margin="30"
                                               Text="{TemplateBinding Title}"
                                               Foreground="Black"
                                               FontWeight="Bold"/>
                                    <!--Window Buttons-->
                                    <StackPanel HorizontalAlignment="Right"
                                                Orientation="Horizontal">
                                        <Button x:Name="MinimizeButton"
                                                Command="{Binding DoSomething}"
                                                Content="Min"
                                                FontSize="11"
                                                Style="{StaticResource WindowChromeButtonStyle}"/>
                                        <Button x:Name="MaximizeButton"
                                                Command="{Binding DoSomething}"
                                                Content="Max"
                                                FontSize="11"
                                                Style="{StaticResource WindowChromeRestoreButtonStyle}"/>
                                        <Button x:Name="CloseButton"
                                                Command="{Binding DoSomething}"
                                                Content="Close"
                                                FontSize="11"
                                                Style="{StaticResource WindowChromeCloseButtonStyle}"/>

                                    </StackPanel>
                                </Grid>
                                <!--#endregion-->
                                
                                <!--#region Window Content-->
                                <Grid
                                    Grid.RowSpan="2"
                                    Grid.Column="0">
                                    <Grid.Background>
                                        <ImageBrush
                                            x:Name="ImageBrush"                                           
                                            Stretch="UniformToFill"
                                            />
                                    </Grid.Background>
                                    <Border Name="Border">
                                        <Border.Background>
                                            <SolidColorBrush
                                                Color="Black"
                                                Opacity=".55"/>
                                        </Border.Background>
                                    </Border>
                                </Grid>
                                <Grid 
                                    Grid.ColumnSpan="2"
                                    Grid.Row="2">
                                    <AdornerDecorator>
                                        <ContentPresenter/>
                                    </AdornerDecorator>
                                </Grid>
                                <!--#endregion-->
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames
                                            Storyboard.TargetName="ImageBrush"
                                            Storyboard.TargetProperty="ImageSource"
                                            Duration="0:0:30"
                                            RepeatBehavior="Forever">
                                            <DiscreteObjectKeyFrame
                                                Value="{StaticResource Image_1}"
                                                KeyTime="0:0:0"/>
                                            <DiscreteObjectKeyFrame
                                                Value="{StaticResource Image_2}"
                                                KeyTime="0:0:15"/>
                                            <DiscreteObjectKeyFrame
                                                Value="{StaticResource Image_3}"
                                                KeyTime="0:0:20"/>
                                            <DiscreteObjectKeyFrame
                                                Value="{StaticResource Image_4}"
                                                KeyTime="0:0:25"/>
                                            <DiscreteObjectKeyFrame
                                                Value="{StaticResource Image_5}"
                                                KeyTime="0:0:30"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <Trigger Property="WindowState" Value="Maximized">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="Transparent"/>
                                <Setter TargetName="RootBorder" Property="Padding" Value="7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--#region Left Navigation-->
        <Grid>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="95"/>
            </Grid.RowDefinitions>

            <ScrollViewer 
                Grid.Row="0"
                Margin="25,0"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Aqua"/>
                            <Setter Property="Effect" Value="{StaticResource DropShadowEffect}"/>
                        </Style>
                    </StackPanel.Resources>
                    
                    <TextBlock 
                        Text="Favorites"/>
                    <ItemsControl Margin="0 0 0 17">
                        <cc:NavigationMenuButton Content="Games"
                            Icon="{StaticResource Folder}"
                            Margin="0 5 0 0"/>
                    </ItemsControl>

                    <TextBlock 
                        Text="Remote"/>
                    <ItemsControl Margin="0 0 0 17">
                        <cc:NavigationMenuButton Content="OneDrive"
                            Icon="{StaticResource OneDrive}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Google Drive"
                            Icon="{StaticResource GoogleDrive}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Dropbox"
                            Icon="{StaticResource Dropbox}"
                            Margin="0 5 0 0"/>
                    </ItemsControl>

                    <TextBlock 
                        Text="Libraries"/>
                    <ItemsControl Margin="0 0 0 17">
                        <cc:NavigationMenuButton Content="Desktop"
                            Icon="{StaticResource DesktopFolder}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Documents"
                            Icon="{StaticResource DocumentsFolder}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Downloads"
                            Icon="{StaticResource DownloadsFolder}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Pictures"
                            Icon="{StaticResource PicturesFolder}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Videos"
                            Icon="{StaticResource VideosFolder}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Music"
                            Icon="{StaticResource MusicsFolder}"
                            Margin="0 5 0 0"/>
                    </ItemsControl>

                    <TextBlock 
                        Text="Devices"/>
                    <ItemsControl Margin="0 17 0 0">
                        <cc:NavigationMenuButton Content="Local disk (C:)"
                            Icon="{StaticResource CDrive}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Local disk (D:)"
                            Icon="{StaticResource NormalDrive}"
                            Margin="0 5 0 0"/>
                        <cc:NavigationMenuButton Content="Local disk (E:)"
                            Icon="{StaticResource NormalDrive}"/>
                    </ItemsControl>
                        
                </StackPanel>
            </ScrollViewer>

            <StackPanel
                Grid.Row="1"
                Grid.Column="0">
                <cc:NavigationMenuButton
                    Content="Andre Corporation"
                    Icon="{StaticResource User}"
                    Margin="20 5 0 15"/>
                <cc:NavigationMenuButton
                    Content="Settings"
                    Icon="{StaticResource Settings}"
                    Margin="20 0 0 15"/>
            </StackPanel>
        </Grid>
        <!--#endregion-->
        
        <Grid Grid.Column="1" Margin="10 0 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/><!--Menu-->
                <RowDefinition Height="Auto"/><!--Sub Menu... File Operation button etc.-->
                <RowDefinition Height="50"/><!--Nav buttons and address bar-->
                <RowDefinition/><!--Files list-->
                <RowDefinition Height="Auto"/><!--Footer.. showing file inf and disk space-->
            </Grid.RowDefinitions>
            
            <!--#region Explorer Header-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center">
                    
                    <RadioButton Content="Home" 
                                 IsChecked="True"
                                 Style="{StaticResource MenuHeaderRadioButtonStyle}"/>
                    <RadioButton Content="Computer"
                                 Style="{StaticResource MenuHeaderRadioButtonStyle}"/>
                    <RadioButton Content="View"
                                 Style="{StaticResource MenuHeaderRadioButtonStyle}"/>

                    <ToggleButton x:Name="ToggleExpandCollapseSubMenu"
                                  FontSize="12"
                                  Style="{StaticResource ExpandCollapseToggleButtonStyle}"/>

                </StackPanel>
                
                <!--SearchBox-->
                <StackPanel Grid.Column="1"
                            >
                    
                </StackPanel>
            </Grid>
            <!--#endregion-->
        </Grid>
    </Grid>
    
    
</Window>
